// @ts-nocheck
const className = 'PhotoEditor';

export enum ClassNames {
  pictureCropper = `${className}__picture-cropper`,
  image = `${className}__image`,
  imageContainer = `${className}__image-container`,
  imageWrapper = `${className}__image-wrapper`,
  line = `${className}__line`,
}
